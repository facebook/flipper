version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo This is only here to ensure we do not get a CircleCI failure."

workflows:
  say-hello-workflow:
    jobs:
      - say-hello

